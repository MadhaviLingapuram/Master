const readline = require('readline-sync');

function addTwoDigits(n)
{

    
    
    let num ;
    if(10 <= num <= 100)
    {
        let a = num/10;
        let b= a - num*10;
        let sum = a+b;
    }
    
    return sum;
}
function check2Digits(prompt)
{
    return num ("Enter a two digit number : ");
}
addTwoDigits(num)
{
    check2Digits(prompt);
    return readline.prompt();

}
